[pytest]
# Test discovery paths
testpaths = tests

# Show extra summary info for failed/error/skipped tests
addopts =
    -v
    --tb=short
    --strict-markers
    -q

# Custom markers to avoid unrecognized marker warnings
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    integration: marks tests requiring the DB
